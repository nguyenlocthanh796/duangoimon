# Runtime
fastapi==0.139.0  # Upgraded from 0.115 to bundle patched starlette (CVE-2026-48710: Host header smuggling in starlette 0.52.1)
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg>=3.2,<4
psycopg-binary>=3.2,<4
alembic==1.13.2
redis==5.1.1
pydantic>=2,<3
pydantic-settings==2.5.2
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0,<4.1
python-multipart==0.0.32  # CVE-fixed: 0.0.19 had multipart DoS, path traversal
websockets==13.0

# Structured logging
structlog>=24.4,<25

# Error tracking (optional — add SENTRY_DSN to enable)
sentry-sdk>=2.17,<3

# HTTP client
httpx>=0.27,<1

# ── Dev dependencies (pip install -r requirements-dev.txt instead) ──
# pytest>=8,<9
# pytest-asyncio>=0.24,<1
# coverage>=7,<8
